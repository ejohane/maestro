{
  "name": "maestro",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.1.30",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "turbo": "^2.0.11",
    "typescript": "^5.6.3"
  }
}
